database:
  host: "localhost"
  port: 54320
  user: "postgres"
  password: "postgres"
  name: "competitive_analysis"
  sslmode: "disable"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

app:
  host: "0.0.0.0"
  port: 8080
  mode: "development"

llm:
  provider: "qwen"
  api_key: "${LLM_API_KEY}"
  base_url: "${LLM_BASE_URL}"

asynq:
  redis_host: "localhost:6379"
  redis_password: ""
  redis_db: 1
